# HuggingFace Dataset Extractor v1

> ⚠️ **已废弃** - 此版本存在编码损坏问题，不再维护。请使用 v2（串行版）。

## 原始说明

原始版本：串行处理 + 双 LLM 调用 + 硬编码配置

## 文件说明

- `run_workbook.py` - 主脚本（编码损坏，不可运行）
- `run_agent_type.py` - Agent 类型专项脚本（编码损坏）

## 使用方式

请改用 v2：
```bash
cd ../huggingface-dataset-extractor_v2
python run_workbook.py 工作簿1
```
